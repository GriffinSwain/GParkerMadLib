This program takes multiple user inputs and gives the user three different stories to choose from. It then gives the option for the user to repeat 
the program and select if they want a new set of words or if they want to keep the original set of words they inputted.

Peer Reviewed by:

-
